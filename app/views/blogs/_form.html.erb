<%= render 'blogs/user' %>
<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>
      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="text-center">
  <!-- タイトルを入力するinputタグを表示 -->
  <label for="blog_title"></label><%= form.text_field :title ,id: "title",placeholder: "タイトル" %>
  <!-- 本文入力するtextareタグを表示 -->
  <label for="blog_body"></label><%= form.text_area :body, id: "input-text", rows: 5 ,placeholder: "本文"%>
  </div>
  <div class ="text-right">
    <p id="counter"></p>
  </div>
  <div class="actions">
    <%= form.submit class: "btn btn-primary"%>
  </div>
<% end %>
