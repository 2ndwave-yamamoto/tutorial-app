<table>
    <% comments.each do |comment| %>
      <tr>
        <td>
          <%= simple_format comment.body %>
        </td>
        <td>
          <p><%= comment.created_at.strftime("%Y/%m/%d") %></p>
        </td>
      </tr>
    <% end %>
  </table>